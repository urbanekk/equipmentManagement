<UserControl x:Class="equipmentMangement.Views.ReservationBasicInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:equipmentMangement.Views"
             xmlns:vm="clr-namespace:equipmentMangement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <vm:ReservationBasicInfoVM x:Key="reservationBasicInfoVM"/>
    </UserControl.Resources>

    <Grid DataContext="{Binding Source={StaticResource reservationBasicInfoVM}}">
        <DataGrid x:Name="reservationInfoDataGrid" Margin="10,10,10,40" ItemsSource="{Binding Reservations, Mode=TwoWay}"/>
        <Button x:Name="loadData" Content="Load data" Margin="10,10,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="80" Command="{Binding LoadData}"/>
    </Grid>
</UserControl>
